/**
 * Reveal animation wrapper.
 * Animates children into view on scroll using GSAP or Framer Motion.
 * Implementation will be added in Phase 3.
 */
export default function Reveal({ children }) {
  return <div>{children}</div>;
}
